package test.java.java_class.Objects;

public class MathStaticTest {
    public static void main(String[] args) {
        //java.lang.Math math =new java.lang.Math();
       //int sum= java.lang.Math.sumNum(1,2,3,4,5,6,7);
        //System.out.println(sum);
       //We can use the array for varargs but in array parameter we cannot give the values directly.
        //we should create the array and call the arrray in the parameter
       int[] nums={1,2,3,4,5,6,7};
       // System.out.println(math.sum(nums));
        //new java.lang.Math().sum(nums);

    }
}
